# Copy to .env. The default matches docker-compose.yml, so `docker compose up -d`
# then `npx prisma migrate dev` works with no edits.
DATABASE_URL="postgresql://ralla:ralla_dev_only@localhost:5432/ralla?schema=public"
